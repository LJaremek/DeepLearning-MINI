# Repo treee

```
.
├───data
│   └───bedroom             <- dir for folders with images
│
├───data_flat_resized       <- original data resized to (64, 64)
│
├───model_paths             <- models paths
│   └───all_data_X          <- trained on all dataset. X is epoch
│
├───train_model.ipynb       <- notebook for training model
├───generate_image.ipynb    <- notebook for generating images
├───resize_train_data.ipynb <- resizing original data to (64, 64)
└───calc_fid.ipynb          <- FID value calcutor
```
